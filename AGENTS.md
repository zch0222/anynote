# AGENTS.md

本仓库的 AI 编程助手规约（Claude Code、Codex、其他 Agent）统一由 [`CLAUDE.md`](./CLAUDE.md) 维护。

请直接阅读 `CLAUDE.md`，包含：

- 仓库定位与 polyglot monorepo 结构
- 当前重构进度（Phase 0-7 状态）
- 常用命令（全栈启动、单服务构建、测试）
- 后端服务端口表
- 三语言代码规范（Java / 前端 / Python）
- REST API 命名规范
- 关键架构约束（OpenAPI Contract-First、HMAC 内部鉴权、Nacos 配置、Gateway 数据源陷阱、ResData 响应格式）
- 提交与分支模型
- 禁止清单

更细的主题文档（架构速查、API 契约、OpenSpec 工作流）入口也在 `CLAUDE.md` 末尾的"上下文文档导航"小节。

> 维护约定：本文件保持为指针，**不要在此追加规约内容**。所有规约写到 `CLAUDE.md`，避免两份漂移。
