# 知识库概览页（D-02）还原改造

> 日期：2026-09-16
> 分支：`dev`（本次改动直接落在 `dev`）
> 设计依据：[`docs/ui/UI补稿设计图.md`](../ui/UI补稿设计图.md) D-02（32 条图例）+ [`docs/ui/UI补稿落地改造方案.md`](../ui/UI补稿落地改造方案.md) 任务 12.2.2
> 相关 changelist：[`2026-09-16-ui-supplement.md`](./2026-09-16-ui-supplement.md)（M12 全量落地）

## 概览

用户反馈「知识库概览页面没有完全还原」。核对画板后确认不是细节偏差，而是**结构缺失**：
D-02 要的是「头图卡片 + 5 格计数 + 三块预览」，而当时的实现只有一行标题与一句简介。

本次按画板逐像素测量后重做该页。核对过程中另外发现并修掉 3 处真实缺陷：
**画板比对工具本身是坏的**（参考图全部错位、且被压在内容上的标注记号污染，见第四节）、
**库不存在时渲染一屏假数据**、**概览路由继承了笔记 Tab 的加载骨架**（后两条见第一节末）。

| 类别 | 文件数 | 增删行 |
|------|--------|--------|
| 新增源码 | 4 | +818 |
| 新增测试 | 3 | +781 |
| 修改源码（含测试与 E2E） | 13 | +573 / −338 |
| 重新生成参考图 | 28 | 二进制 |
| 新增参考图 | 12 | 二进制 |

分两批提交：首批 4 个 commit（59 个文件），复核阶段追加 1 个 commit（7 个文件，
`+312 / −24`）。合计 66 个文件，其中代码与文档 26 个、二进制参考图 40 个。

数字口径：`git diff --stat <首个 commit>^..HEAD`（已跟踪文件）+ `git ls-files --others`
（新增文件，逐个 `wc -l`）。下表的「文件数 / 增删行」按**源码与测试分行**列出，
参考图只记张数——它们的字节数不构成有效信息。

### 验证结果

| 命令 | 结果 |
|------|------|
| `npx tsc --noEmit`（apps/web） | 通过，无输出 |
| `npx vitest run`（apps/web） | **151 文件 / 1761 用例全部通过**（改前 148 / 1696） |
| `npx playwright test`（全量，含移动端） | 共跑 6 轮：**4 轮 126/126 全绿**，2 轮各出现 1–2 条偶发（均非本批引入，见下）。`workers: 1` 串行；改前 125 条 |
| `npx playwright test ui-supplement.spec.ts --project=chromium` | **24 通过 / 0 失败**（其中新增 D-02 7 条）|
| `npx playwright test ... -g "D-02"`（连跑 18 轮） | 17 轮 7/7；1 轮 6/7 后修掉根因（见下），修后连跑 5 轮均 6.4–6.8s 稳定 |
| `npx playwright test ui-supplement.spec.ts loading-system.spec.ts` | 39 通过 / 0 失败 |
| `npx playwright test ui-redesign.spec.ts notes.spec.ts theme.spec.ts` | 26 通过 / 0 失败 |
| `pnpm --filter web bundle:budget` | 全绿：`/notes/[baseId]/overview` 首屏 298.6 KB / 预算 310 KB；移动端 249.9 / 250 KB；编辑器 chunk 14.1 / 250 KB |
| `pnpm check`（Biome） | 通过（609 文件） |
| 真实浏览器几何实测 | 见下「还原度证据」 |

> **关于全量 E2E 的偶发失败（如实记录，均与本批改动无关）**：
> 6 轮里 4 轮全绿；另 2 轮出现过 `ui-supplement` D-10（协同文档库删除，等 WebSocket
> 索引房间同步）与 `cli-authorize` 两条（报表为 `Test timeout of 60000ms exceeded`）。
> 两者的共同点是**单跑必过**（D-10 单跑 3/3、`cli-authorize.spec.ts` 单跑 5/5），
> 且 `git diff --name-only` 确认本批**未触碰** `/docs`、协同服务、CLI 与授权页的任何文件。
> `cli-authorize` 的根因在 [`2026-09-14-route-progress-no-layout-shift.md`](./2026-09-14-route-progress-no-layout-shift.md)
> 第 5 条已记录过（`NODE_EXTRA_CA_CERTS` 未设 / Playwright 写 trace 产物 `ENOENT`），
> 属既有的环境问题。仓库既有 changelist 也反复记录过"多个 spec 共用写死的知识库名、
> 多轮运行累积数据"这一干扰源。
> （该用例注释里已记录过同类现象），与本次改动无关，详见「未完成项与偏差」第 1 条。

### 还原度证据（实测，非目测）

画板内区经机器量出为 `crop={left:80,top:372,width:2880,height:1800}` = **1440×900 CSS**（2 倍图）。
在 1440×900 视口下用真实浏览器测实现，与画板逐项对照：

| 项 | 画板 | 实现 |
|----|------|------|
| 侧栏宽 | 296 | 296 ✓ |
| 内容列 | 368..1367（1000 宽） | 368..1367 ✓ |
| 头图卡片 | y 28..264（高 237） | y 28..265（高 237）✓ |
| 封面 | 976×96，距卡顶/左各 12，圆角 10 | 976×96，12 / 12，10 ✓ |
| 标题 H1 | Display 34/41 SemiBold，墨迹 y155..187 | 34 / 41 / 600，y155..186 ✓ |
| 5 格卡片 | 190×98，色块 28×28 圆角 7 距边 14 | 190×98，28×28 / 7 / 14 ✓ |
| 色块颜色 | 笔记 `#0071e3` / 慕课 `#af52de` / 任务 `#ff9500` / 资料 `#5856d6` / 成员 `#34c759` | 同 ✓ |
| 左列预览卡 | x 368..967（600 宽），y 439..698 | 600 宽，y 439..699 ✓ |
| 右列预览卡 | x 988..1367（380 宽），列间距 20 | 380 宽，间距 20 ✓ |
| 笔记行高 | 52（含 1px 分隔线） | 51+1 ✓ |
| 深色三色 | 页面 `#000000` / 侧栏 `#17171a` / 卡片 `#1c1c1e` | 逐像素相同 ✓ |
| 加载骨架（图例 26） | 封面 + 标题 + 两行文本 + 5 格，与真实版式同高 | 延迟 RSC 实测：`976x96@380,40`、`256x41@388,152`、`640x24@388,199`、`192x18@388,229`、**`190x98@368,285`** —— 5 格落点 y=285 与真实内容**完全相同** ✓ |

## 一、概览页（`apps/web/src/features/notes/`）

整页按 D-02 重做。原实现位于 `knowledge-base-detail.tsx` 内，与资料 / 成员两个 Tab
共处一个文件；本次拆出独立文件，因为概览是**唯一一页订阅六棵查询**的页面，
继续挤在详情文件里会让那个文件同时承担三种完全不同的版式。

| 文件 | 状态 | 作用与原因 |
|------|------|-----------|
| `components/knowledge-base-overview.tsx` | 新增 | D-02 整页。头图卡片（封面 976×96 / 标题 / 类型徽标 / 简介 / 元信息 / **动作行**）+ 5 格计数 + 左「最近笔记」右「资料 / 成员」。三块预览各自只取前 5 / 3 / 3 条（图例 18/19/23），每块带「全部 X」链接直达对应 Tab。**详情查询 `isError` 时整页换成 `NotFoundState`**（见下「两处真实缺陷」第 1 条） |
| `lib/overview-sections.ts` | 新增 | 5 格的**单一事实源**：`OVERVIEW_TILES` 派生自侧栏的 `knowledgeBaseSections`，所以「与侧栏二级导航一一对应」（图例 10 原话）是结构保证而不是巧合。`tileCountText()` 把 `undefined` / `null` / `NaN` / `Infinity` 一律折成 `null`，交给调用方渲染骨架——先显示「0 篇笔记」等于告诉用户这个库是空的 |
| `components/knowledge-base-detail.tsx` | 修改 | 移出概览相关代码（`KnowledgeBaseOverview` / `OVERVIEW_PREVIEW` / `CreateNoteDialog` 的 dynamic 块）与随之无用的 import，只留 `KnowledgeBaseDocs` / `KnowledgeBaseMembers` / `permissionLabel` / `KnowledgeBaseMissing` |
| `components/__tests__/knowledge-base-overview.test.tsx` | 新增 | 27 条。六棵查询全部在 **hook 层**打桩（不 mock 全局 fetch）。覆盖：头图各字段与空值回退、权限 1/2/3/4/undefined 五态、动作行唯一性、5 格的 href 与计数、骨架而非 0、预览截断与行链接、资料行不可点、空态 / 错误态、**不存在 / 无权限三态（含"加载中不误判为不存在"）**、查询复用 |
| `lib/__tests__/overview-sections.test.ts` | 新增 | 10 条。钉住 5 格与侧栏的**顺序与文案一致**、色块各不相同、资料用 `bg-indigo`、`tileCountText` 的边界 |
| `components/__tests__/knowledge-base-detail.test.tsx` | 修改 | 概览的用例整体迁到新文件；import 改为只取 `KnowledgeBaseDocs`，并移除随之无用的 `useKnowledgeBaseQuery` 桩。余 7 条 |

### 另外两处真实缺陷（本轮由真实浏览器实测暴露并修复）

首轮提交后继续逐条核对图例，发现两处**不是"画得不像"、而是"功能不对"**的问题：

| 问题 | 实测证据 | 修复 |
|------|---------|------|
| **库不存在时渲染一屏假数据** | `/notes/999999/overview` 后端返回 `{code:"A0301"}`（HTTP 200，靠 code 区分）→ 详情查询 `isError`，而其余五棵查询照样"成功"返回空列表。修复前页面照常渲染整页：头图写「未命名知识库」、5 格计数写 0、简介写「这个知识库还没有填写简介。」——**用户完全看不出这个库不存在**。同级 5 个 Tab 在同样输入下都不是这样 | 详情 `isError` 时最外层短路，渲染共用组件 `NotFoundState`（文案已在方案 12.0.3 定稿：找不到这个知识库 / 它可能已被删除，或者你还没有访问权限 / 回到知识库）。注意 `KnowledgeBaseMissing` 是**从未被任何地方引用的死代码**，本次改用统一的 `NotFoundState` 而不是复活它 |
| **概览路由继承了笔记 Tab 的加载骨架** | 图例 26 要求「加载骨架 · 封面 + 标题 + 两行文本 + 5 格计数，与真实版式同高」。而该路由段没有自己的 `loading.tsx`，按 Next 的就近规则继承了 `[baseId]/loading.tsx` 的**行列表**骨架——真实浏览器延迟 RSC 实测：过渡态 18 个灰块中**没有一个**落在头图或 5 格的位置上，数据到达时整页从"一列行"换成"卡片 + 网格" | 新增 `overview/loading.tsx` + 共用预设 `KnowledgeBaseOverviewSkeleton`，尺寸逐项对齐画板实测值（卡片 1000 宽 / 封面 96 / 5 格各 98 / 预览区同栅格断点）。修复后实测过渡态：`976x96@380,40`、标题、两行文本、`190x98@368,266`——与真实内容落点一致 |

这两条都按仓库规约**先写复现用例再改代码**：单测新增 3 条（第一条缺陷）+ 4 条（第二条缺陷），
E2E 新增 1 条（第一条缺陷，跑真实栈）。

> **本轮我自己也犯过一次同类错误，如实记下**：新写的 `KnowledgeBaseOverviewSkeleton`
> 第一版把文本块写成 `h-8 / h-4 / h-3` + `space-y-2`，三行加起来 98，整卡 218；
> 而真实卡片是 **237**（12+96+117+12）——**差 19px**，正好是图例 26 那句
> 「与真实版式同高」要防的跳。
>
> 值得记的是它**当时所有测试都是绿的**：单测只检查槽位与类名存在。
> 是我拿真实浏览器量了骨架的落点（`190x98@368,266`）再与真实内容（`y=285`）对比
> 才发现。修法是按**行盒**给高而不是凑整数：标题 41（Display 34/41）、
> 简介 24（Body 15/24）、元信息 18（Footnote 13/18）。
> 修完实测 `190x98@368,285`，与真实内容完全重合。
> 同时补了一条锁死这三个行盒的回归用例。

## 二、外壳（`apps/web/src/components/layout/`）

概览页在画板里**没有 56 高的顶栏**：搜索（图例 7）、主题（图例 8）、「新建笔记」（图例 9）
三个动作都画在头图卡片的动作行里。顶栏若照常渲染，同一屏会出现两套「切换主题」按钮——
读屏会念两遍，`getByRole("button", { name: "切换主题" })` 直接变成 strict mode violation。
这与仓库里既有的结论（「两处都放等于两处都不像主导航」）是同一类问题。

侧栏宽度此前是全局写死的 256。实测画板分两档：知识库内 **296**（要放下 264 宽的库卡片），
库外 **272**。

| 文件 | 状态 | 作用与原因 |
|------|------|-----------|
| `components/layout/navigation.ts` | 修改 | 新增 `isKnowledgeBaseOverviewRoute()`。用**完整段匹配** `/^\/notes\/\d+\/overview$/`：`/notes/7/overviewX` 不是概览页 |
| `components/layout/app-shell.tsx` | 修改 | 概览页不渲染 `AppHeader` 与 `RouteProgressBar`（后者存在的意义就是"紧跟顶栏"）。同时给内容区补 `pt-7`——画板内容从 y=27 起，常规页面那 56 高的顶栏本身承担了这段留白，去掉顶栏后必须显式补回，否则整页比画板高 27px |
| `components/layout/app-sidebar.tsx` | 修改 | 侧栏宽度按路由分两档（库内 296 / 库外 272），并加 `data-width` 便于 E2E 断言。用完整类名字面量而非拼接字符串：Tailwind 的 JIT 只扫描字面量，拼出来的类名不会进产物 |
| `components/layout/knowledge-base-sidebar.tsx` | 修改 | 库卡片按图例 2 改为 264×48（封面 36、圆角 10、白底 shadow-card），文字从 body/footnote 降到 footnote/11px。卡片比导航项多缩 6px（画板里卡片左缘 16、导航项 tint 从 10 起） |
| `components/layout/__tests__/navigation.test.ts` | 修改 | +3 条：只有 `/notes/{id}/overview` 命中；同级 Tab 与编辑器都不命中；尾随段 / 前缀 / 非数字 id 不命中 |
| `components/layout/__tests__/app-shell.test.tsx` | 修改 | +2 条：概览页无顶栏且侧栏不受影响；概览页之外顶栏仍在 |

## 三、Token（`apps/web/src/app/globals.css`）

| 文件 | 状态 | 作用与原因 |
|------|------|-----------|
| `app/globals.css` | 修改 | 新增 `--state-indigo: #5856d6` 与 `--color-indigo` 映射。D-02 图例 14 给「资料」色块指定了**靛蓝 #5856D6**，而这个值此前不在 Token 表里（其他四个色块都能复用已有语义色）。深色沿用同一个值：靛蓝在深色下对比度足够，画板深色版也是同一支 |

## 四、画板比对工具（`apps/web/scripts/`）

这一节修的是**工具缺陷**，不是概览页本身。但它决定了"截图对比"这件事是否可信，
所以必须记下来：修之前，所有画板的参考图都是错位的。

原裁剪表是**手填**的：每个画板一行 `{left:140, top:300, width:2680, height:1230}`、
期望 `1340×615`。而实测屏幕内区是 `{80,372,2880,1800}` = **1440×900**。
后果不是"对比图略偏"——参考图左边多出侧栏外空白、右边裁掉了内容，
缩放后**两边字号不一致**，人眼看到的差异全是裁剪误差造成的假象。

另有一处更隐蔽的污染：画板上用**品红（`#e01e80`）/ 紫罗兰（`#b969e2`）标注记号**
标编号与引线，这些记号**压在屏幕内容上**。逐像素测量会把记号本身当成内容——
实测「最近笔记」标题行墨迹从 y394 起，而 y394..395 那两行其实是品红角标，
标题真正从 y409 才开始。不清理就会得出"标题位置错了 15px"这种假结论。

| 文件 | 状态 | 作用与原因 |
|------|------|-----------|
| `scripts/lib/supplement-crops.mjs` | 新增 | 把"怎么量出屏幕内区"写成可测代码。判据：窗口边框色恒为 `#d8d8de`（2 设备像素宽）→ 找竖线 → 按**已知视口宽**（2880/780）推右缘并验证 → 行覆盖率找顶边 → **高度查表**。最后一条最关键：画板上的屏常比视口高（实测有 1440×1225、390×1001），量出多高就裁多高会得到与截图对不齐的参考图。同时导出 `isAnnotationMarker()` 清理标注记号 |
| `scripts/lib/__tests__/supplement-crops.test.mjs` | 新增 | 17 条。**每一条都对应实现里真踩过的坑**：内部竖直分隔线（D-05/07/10/13 同一块屏上下展示两态，只配相邻列段会算出 1454 宽而整块丢弃）、状态缩略图混入（取"第一条/最后一条"边框行会把缩略图圈进来）、圆角导致的连通性断裂、长图截断、多台手机排序、记号残影让卡片宽度读成 179（真值 190） |
| `scripts/extract-supplement-reference.mjs` | 修改 | 裁剪框改为**运行时从画板量出**，不再手填——数字只有一个来源（画板本身），不存在"改了画板忘了改表"。量不出屏时**失败退出**而不是回退到默认框（回退正是当初错位事故的形态）。裁图复用已清洗的像素缓冲，记号不会留在参考图里 |

### 重新生成的参考图

`apps/web/e2e/reference/supplement/` 下 28 张已跟踪参考图全部重生（尺寸从 `1340×615`
修正为 `1440×900` / `390×844`），并新增 12 张此前生成但从未入库的（M-04 / M-05 /
M-07 / M-08 / M-10 / M-11 的浅深两态）。

生成命令：`node apps/web/scripts/extract-supplement-reference.mjs`

## 五、E2E（`apps/web/e2e/`）

| 文件 | 状态 | 作用与原因 |
|------|------|-----------|
| `e2e/ui-supplement.spec.ts` | 修改 | 新增 D-02 章节 7 条，断言的是**画板实测数字**而不是"看着差不多"：头图卡片 1000 宽 / 封面 976×96 / 内边距 12 / 卡片圆角 14 / H1 是 34-41-600；概览页无顶栏且全页只有一份搜索与主题按钮（单数 `getByRole` 本身就是断言）；5 格 190×98、色块 28×28 圆角 7 颜色 `#0071e3`、href 与侧栏逐项一致；预览卡 600 / 380 宽间距 20、笔记行 51+1、成员行 52；区块标题 17/22/600 与「全部 X」13px；主按钮 34 高 accent 实心并能打开对话框；**库不存在时整页换成「找不到这个知识库」且不出现任何假数据**<br>**`test.use({ viewport: { width: 1440, height: 900 } })` 是必需的**：Playwright 的 Desktop Chrome 预设是 1280×720，而本页内容列是 `max-w-[1000px]` 居中，1280 下只剩 920 宽，所有几何断言会整体差 80 |

> 有一条用例的名称从「资料 3 行 x56、成员 3 行 x52」改成了它真正断言的内容。
> 原标题承诺了两个它**并没有断言**的规格——这是"名字比断言大"的假绿，
> 评审时只看用例列表会以为行高已被覆盖。真实栈上造不出资料数据
> （只有上传端点，没有"创建一条文档记录"的接口），所以资料行高改由单测覆盖，
> E2E 只断言新库必然出现的空态与「去上传」链接。

## 审计要点

1. **`scripts/lib/supplement-crops.mjs` 的「高度查表」是本次最容易被误当成多余的一行。**
   它看起来像常量表，实际是唯一能保证参考图与浏览器截图可比的机制。删掉它、
   改回"量出多高裁多高"，并排图会一边长一边短，而**所有几何断言仍然全绿**——
   因为断言量的是实现，不是参考图。要看的是那份测试里"屏比视口高时只取顶部视口高度"。
2. **概览页不渲染顶栏是个有代价的决定。** 它让"概览"成为唯一没有顶栏的工作区页面，
   `app-shell.tsx` 因此多了一个分支。代价换来的是画板要求的"卡片即页头"，
   以及同一屏不会出现两套同名控件。若要回退，`isKnowledgeBaseOverviewRoute` 是唯一开关。
3. **`pt-7` 不是随手加的魔法数。** 去掉顶栏后内容会贴到窗口上沿（画板从 y=27 起），
   这 28px 补的是顶栏原本承担的留白。改顶栏高度时必须同时看这里。
4. **5 格的两个易错点都写进了测试**：数据未到时给骨架而不是 0（先显示「0 篇笔记」
   等于告诉用户库是空的）；"与侧栏一一对应"用 href 集合比对而不是下标——下标配对
   在导航增删一项时会静默错位，配上 `toHaveCount(1)` 恰好还能过，是最糟的假绿。
5. **`knowledge-base-detail.tsx` 少了 113 行**（−316/+213 的一部分）。它现在只服务
   资料与成员两个 Tab。评审时可以顺手确认没有别处仍在 import 已移出的符号
   （`tsc --noEmit` 已确认，但那是编译期检查，看不到"语义上该用新页却仍在用旧页"）。
6. **侧栏宽度分档只改了一处判断。** 若后续新增非 `/notes/{id}` 形态的知识库路由
   （例如组织知识库走 `/orgs/{id}/...`），`parseBaseIdFromPath` 不认，侧栏会退回 272
   而库卡片需要 296——那时要扩展的是这个函数，不是加第二处判断。
7. **`KnowledgeBaseMissing` 现在仍是死代码。** 它实现的就是"找不到这个知识库"兜底态
   （D-02 图例要求的那一个），但**从未被任何地方引用过**——这本身就是它为什么
   没拦住「库不存在渲染假数据」那条缺陷的原因。本次改用方案 12.0.3 定稿的共用组件
   `NotFoundState`（文案与它一致；该组件已在 8 处服务慕课详情、任务详情、协同工作区
   与本次的概览页），没有复活它。
   评审时值得确认：是否该把它删掉，避免下一个页面又误以为"兜底已经有了"。
8. **骨架形状是"与宿主同高"这一条最容易被简化掉，本次已经踩过一次。**
   `KnowledgeBaseOverviewSkeleton` 里的 `h-[98px]` / `h-[41px]` / `h-[260px]`
   看起来像硬编码魔数，其实是**画板实测的真实高度**（或真实行盒）。
   第一版我把文本块写成 `h-8 / h-4 / h-3`，整卡比真实矮 19px，
   而**当时所有单测都是绿的**——它们只检查槽位与类名存在。
   现在 `skeletons.test.tsx` 里那条用例锁死了三个行盒的具体值，
   评审时请看它是否还在，别把它当成"断言 class 字符串"的形式主义。
9. **`loading.tsx` 是按路由段就近生效的，加页面时容易漏。**
   概览段此前没有自己的 `loading.tsx`，于是继承了笔记 Tab 的行骨架（第二条缺陷）。
   本轮只补了概览这一段；资料 / 成员 / 慕课 / 任务各段的加载态形状是否也对得上宿主，
   **没有逐段核对**——那是另一批工作，记在「未完成项」第 6 条。
10. **新增的 D-02 用例里有一处不必要的等待，已删掉——它是自己连跑才发现的。**
    六个用例原来都先 `await expect(kb-gallery).toBeVisible({ timeout: 30_000 })` 再建库，
    但 `createBase` 只用同源 `fetch`，**不需要画廊渲染完**。这一等把用例绑在画廊的
    加载耗时上（连跑 18 轮里有 1 轮吃掉 30s 超时，同一套用例平时只要 6.5s）。
    删掉后连跑 5 轮稳定在 6.4–6.8s。
    值得记的理由：**"多等一个不相关的东西"是 E2E 最常见的偶发与假绿来源**，
    它平时不报错、只在机器慢时红，很容易被当成"环境问题"糊过去。

## 未完成项与偏差（如实记录）

1. **全量 E2E 有偶发失败，均与本批无关。** 6 轮里 4 轮 126/126 全绿，另 2 轮出现
   `ui-supplement` D-10（协同文档库删除）与 `cli-authorize` 两条（`Test timeout of 60000ms exceeded`）。
   两者**单跑必过**（D-10 单跑 3/3、`cli-authorize.spec.ts` 单跑 5/5），
   且 `git diff --name-only` 确认本批未触碰 `/docs`、协同服务、CLI 与授权页。
   `cli-authorize` 的根因在 [`2026-09-14-route-progress-no-layout-shift.md`](./2026-09-14-route-progress-no-layout-shift.md)
   第 5 条已记录（`NODE_EXTRA_CA_CERTS` 未设 / Playwright 写 trace 产物 `ENOENT`）。
   **没有修它们**是因为超出本次授权范围（用户要求的是概览页还原）。
   仓库既有 changelist 也反复记录过"多个 spec 共用写死的知识库名、多轮运行累积数据"
   这一干扰源——要根治得改成各自建临时库，那要动 5 个既有 spec 的造数方式。

2. **只读成员分支没有真实栈 E2E。** 图例 9 要求「新建笔记」在可阅读及以下权限隐藏，
   但要造一个只读成员需要另一套账号 + 成员关系。E2E 只断言了可编辑时的正向行为，
   权限分支由单测覆盖（`permissions` 1/2/3/4/undefined 五种）。
   这是**覆盖率上的缺口**，不是未实现。

3. **「最近笔记」的条数上限是 5，但列表查询用的是默认页大小。** 当默认页大小小于 5
   时预览会少几条。当前 `DEFAULT_PAGE_SIZE` 大于 5，所以没有实际影响；
   若将来下调它，这里会静默变少——没有为此加断言，因为那时更该做的是
   让预览单独请求 `pageSize=5`。

4. **画板上的品红/紫罗兰记号是按色相识别的，不是按图层。** 也就是说，
   如果设计里真的出现同色系的正常内容（例如紫罗兰 `/ai` 品牌色），它会被一并抹成白色。
   本次核对了 D-xx / M-xx 全部 40 张画板，未发现同色内容；这个风险记为已知限制，
   写在 `isAnnotationMarker` 的注释里。

5. **`/m/*` 移动端概览页不在本次范围。** D-02 是桌面画板；移动端知识库内是
   另外三块（M-03 慕课 / M-04 任务 / M-05 资料），没有概览页。

6. **只核对了概览段的加载骨架，其余路由段的骨架形状没有逐段验证。**
   `loading.tsx` 按路由段就近生效，`[baseId]/loading.tsx` 是笔记 Tab 的行骨架，
   概览段此前正是因此继承了它（第二条缺陷）。同一个机制下，
   资料 / 成员 / 慕课 / 任务各段的加载态是否也对得上各自宿主，
   **本轮没有核对**——它们的画板（D-05 / D-07 / D-08 / D-09）也有对应的加载态要求。
   这是本次授权范围内的**已知遗留**，建议下一批按 D-02 同样的方法逐段量一遍。

7. **`KnowledgeBaseMissing` 留作死代码未删。** 它实现的就是 D-02 要求的不存在兜底态，
   却从未被引用——删除它属于清理而非还原，且 `knowledge-base-detail.tsx` 的
   导出面变化会影响其它 import 路径。本次只改用统一的 `NotFoundState`，没有删它。
